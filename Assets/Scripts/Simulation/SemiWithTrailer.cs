using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//The semi with trailer consists of two gameobjects, so to get each one we need this script
public class SemiWithTrailer : MonoBehaviour 
{
    public Transform semiTrans;
    public Transform trailerTrans;
}
